{
  "name": "lambda-ui",
  "theme": "ui",
  "version": "0.11.0",
  "description": "Minimalist dark low contrast theme",
  "license": "MIT",
  "repository": "https://github.com/mattstyles/lambda-ui",
  "scripts": {
    "prepublish": "rm -rf ./styles && cp -r ./stylesheets ./styles"
  },
  "engines": {
    "atom": ">0.50.0"
  }
}
